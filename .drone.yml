---
name: Build and Publish (Alpha Release)
kind: pipeline
type: docker
environment:
  pkgname: mpm-alpha
  release-type: alpha
  nexus_repository_password:
    from_secret: nexus_repository_password
  DEBIAN_FRONTEND: noninteractive

triggers:
  event:
    - push
  branch:
    - alpha
  paths:
    - src/PKGBUILD
    - .trigger

steps:
  - name: build
    image: ubuntu
    commands:
      - scripts/build.sh
---
kind: signature
hmac: 3f4fb7f02c6f8df8ab378bc4089c1ad686ef0a0964b3a98ed442f88b19ba0206

...
