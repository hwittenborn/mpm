---
name: Build and Publish (Alpha Release)
kind: pipeline
type: docker
environment:
#   pkgname: mpm-alpha
#   release-type: alpha
  nexus_repository_password:
    from_secret: nexus_repository_password
#   DEBIAN_FRONTEND: noninteractive

trigger:
  event:
    - push
  branch:
    - alpha
  paths:
    - src/PKGBUILD
    - .trigger

steps:
  - name: build
    image: ubuntu
    commands:
      - scripts/build.sh
---
kind: signature
hmac: 7dba4a0ca495722f70b7247d707e8d380192561f7346d9c4b3cea31f99963cbf

...
