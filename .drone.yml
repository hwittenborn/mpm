---
name: Build and Publish (Alpha Release)
kind: pipeline
type: docker
environment:
  test: testing
#   pkgname: mpm-alpha
#   release-type: alpha
#  nexus_repository_password:
#    from_secret: nexus_repository_password
#   DEBIAN_FRONTEND: noninteractive

trigger:
  event:
    - push
  branch:
    - alpha
  paths:
    - src/PKGBUILD
    - .trigger

steps:
  - name: build
    image: ubuntu
    commands:
      - scripts/build.sh
---
kind: signature
hmac: 3f33471f946742bf2a4adfc144ed13552d2dacd21513e2c11160f12053ae6f73

...
