name: Build and Publish
kind: pipeline
type: docker
environment:
  release_type: alpha
  DPKG_FRONTEND: noninteractive

steps:
  - name: build
    image: ubuntu
    commands:
      - scripts/build.sh
